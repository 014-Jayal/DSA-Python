class Solution:
    def missingRange(self, arr, low, high):
        
        sarr = set(arr)
        result = []
        
        for i in range(low, high+1):
            if i not in sarr:
                result.append(i) 
                
        return result
